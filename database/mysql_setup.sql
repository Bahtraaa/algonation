-- MySQL setup for Laravel app
-- Run this in MySQL to create database and switch to it

CREATE DATABASE IF NOT EXISTS perabotan_rumahku CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE perabotan_rumahku;

-- Optional: create a dedicated user
-- CREATE USER IF NOT EXISTS 'laravel'@'localhost' IDENTIFIED BY 'secret';
-- GRANT ALL PRIVILEGES ON perabotan_rumahku.* TO 'laravel'@'localhost';
-- FLUSH PRIVILEGES;
